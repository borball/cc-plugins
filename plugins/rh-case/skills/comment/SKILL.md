---
description: "Add a comment to a Red Hat support case"
---

Add a comment to a support case. Arguments: $ARGUMENTS

**Confirm with the user before posting.**

Run:
```bash
CLAUDE_PLUGIN_DATA="${CLAUDE_PLUGIN_DATA}" ${CLAUDE_PLUGIN_ROOT}/scripts/rh-case-comment.sh $ARGUMENTS
```
