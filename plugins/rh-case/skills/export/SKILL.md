---
description: "Export a Red Hat support case to markdown"
---

Export a support case to markdown. Arguments: $ARGUMENTS

Run:
```bash
CLAUDE_PLUGIN_DATA="${CLAUDE_PLUGIN_DATA}" ${CLAUDE_PLUGIN_ROOT}/scripts/rh-case-export.sh $ARGUMENTS
```
